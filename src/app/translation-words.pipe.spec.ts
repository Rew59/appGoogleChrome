import { TranslationWordsPipe } from './translation-words.pipe';

describe('TranslationWordPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslationWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
